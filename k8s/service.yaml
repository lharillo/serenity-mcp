apiVersion: v1
kind: Service
metadata:
  name: serenity-mcp
  labels:
    app: serenity-mcp
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: serenity-mcp
